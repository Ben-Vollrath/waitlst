name: test-backend

on:
  push:
    branches:
      - "**"

jobs:
  deploy-function:
    runs-on: ubuntu-latest

    steps:
      - uses: supabase/setup-cli@v1
        with:
          version: latest

      - run: supabase test db
        working-directory: ./backend/
